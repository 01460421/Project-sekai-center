
## title
學測複習整合站：內容撰寫格式與技術實作方案（沿用 Project-sekai-center 靜態棧）

## principles
[
 {
  "name": "源格式：一單元一份 .unit.md（YAML front matter ＋ Markdown ＋ 型別化 fenced 區塊）",
  "rationale": "三個候選各自的取捨很清楚。JSON：TeX 反斜線要雙重跳脫（\\\\frac 寫成 \\\\\\\\frac）、prose 全擠在一行、git diff 完全不可讀、無法留註解，多人協作直接崩潰。直接寫 JS：內容變成程式碼，非工程師不能寫、無法 lint、merge conflict 慘烈、而且會像 app.html 一樣被吸進主 bundle（現在 app.html 已 1.27MB，770KB 是 logic）。MDX 同樣被否決——內容能 import 元件就等於內容是程式碼，會破壞「內容是惰性資料」這條讓整套快取與安全模型成立的性質。Markdown+front matter 是唯一同時滿足量大（純文字、每行可 diff）、數學（$...$ 原樣寫）、圖（相對路徑 + 建置期處理）、交叉引用（[[UNIT-ID#s07]] 建置期解析並驗證）、版控（逐行 review、blame 到人）、協作（學科老師不必碰 JS）的格式。純 Markdown 撐不住的部分——S07 例題七欄位、速記卡、圖表規格——用型別化 fenced 區塊（```problem / ```cardset / ```plot，內容是 YAML/JSON）補上，結構化的地方有結構，散文的地方是散文，仍然一單元一檔。docs/research/內容深度/單元講解規格與範例章節.md 已經把 S01–S14 的規格寫死了，這份格式就是它的載體。實務鐵則：任何含 TeX 的 YAML 欄位一律用區塊純量（key: |），不要用雙引號字串——這是這套格式最常見的一個 bug。",
  "concrete_ui": "作者在 content/math/MATH-H-ALG-QUAD-01.unit.md 裡寫作。站上一個單元頁頂端有「速覽 3 分 / 紮實 30 分 / 深挖」三段式切換（對應 layer 標記投影出的三個視圖，不是三份文件），切換時已渲染的區塊只是顯示/隱藏，不重新抓資料；每個區塊標題右側有錨點連結與「回報這段有問題」小按鈕；[[UNIT-ID]] 交叉引用在畫面上是一顆帶科目色圓點的內文連結，hover 顯示該單元的 S01 一句話定位浮卡。"
 },
 {
  "name": "內容永遠是惰性資料，用 renderVals ＋ dangerouslySetInnerHTML 注入，互動性交給 Custom Elements",
  "rationale": "dc-runtime 的模板表達式（support.js 的 expr.ts，resolve() 只支援識別字、屬性路徑、索引、字串/數字字面值、! 與 ==/===，沒有算術、沒有函式呼叫、沒有三元）根本不可能拿來渲染學科內容；而 {{ }} 插值會把值包進 <span class=\"sc-interp\">，不能輸出 HTML。但 walkText 對 React element 是直接放行的（support.js:603 isValidElement(v) 就原樣渲染），app.html:14369 與 15115 已經在用這條路徑：renderVals() 回 React.createElement('div',{dangerouslySetInnerHTML:{__html:x.a}})。這條路徑就是學科內容的正解——內容完全繞過模板編譯器，dc-runtime 只負責外殼。這同時解決了 Q7 的規模疑慮：模板大小由「頁面型別數」決定，不由「內容量」決定。代價是注入的 HTML 沒有 React 節點、不能掛 onClick；解法不是回頭用 React 重寫內容，而是用 Custom Elements——瀏覽器會在 innerHTML 解析時自動升級自訂元素，所以 <ex-answer>、<ex-fold>、<ex-tree> 在注入的字串裡會自己活起來，完全不需要注入後再掃 DOM 手動綁事件。安全性靠建置期白名單淨化（標籤/屬性 allowlist），不需要執行期 DOMPurify，因為內容全部在 git 裡且經過 lint。一個必守的效能細節：注入用的 React element 必須以 unitId+contentHash 記憶在 this._elCache，否則每次 setState（每個按鍵）都會重新 parse 20KB HTML。",
  "concrete_ui": "單元內文看起來就是一般排版良好的講義：定理框、例題卡、可摺疊的「深挖」段落。例題的「完整解」預設收起，點「看解答」才展開（<ex-answer> 元件，狀態存在元件自己身上，不觸發整頁 re-render）；解題決策樹是一張可點的 SVG（<ex-tree>），點葉節點直接捲到 S07 對應題號並高亮。"
 },
 {
  "name": "數學式在建置期預渲染 KaTeX（htmlAndMathml），執行期零數學函式庫",
  "rationale": "三個選項實測取捨：(a) 執行期 KaTeX ≈ 280KB JS ＋ 字型，還要在每次注入內容後掃描 DOM 再渲染，會產生可見的公式跳動，而且 auto-render 對已注入的 HTML 要重跑一次，成本隨內容量線性上升。(b) 執行期 MathJax 更重（>1MB）且更慢。(c) 預渲染成 SVG：自足、不需字型，但一條顯示公式約 3–8KB 的 path 資料，相對 KaTeX HTML 的約 600B 是 5–10 倍膨脹，在幾十萬字規模下光數學就會多出幾十 MB；而且 SVG 公式不能被選取、不能複製、不隨使用者字級縮放、螢幕閱讀器讀不出來。結論是建置期跑 katex.renderToString({output:'htmlAndMathml'})，輸出視覺用的 HTML span ＋ 語意用的 MathML，執行期只需要 katex.css（約 23KB）與字型。離線與 CSP 都因此變乾淨：不需要 unsafe-eval、不需要外部 CDN、字型自架在 /vendor 底下（目前 vercel.json 完全沒有 CSP header，而 dc-runtime 的 x-import 用 new Function 會要求 unsafe-eval——好消息是 app.html 一次都沒用到 x-import，所以新站可以直接立 script-src 'self'，代價只是重型互動元件要改走 helmet 內的 <script> 或既有的 iframe ?embed= 面板協定）。KaTeX 是 JS 而工具鏈是 Python，做法是 Python 當管線 driver、把整批公式一次丟給單一 node 子行程（stdin JSON in / stdout JSON out），而不是每條公式開一次 node。",
  "concrete_ui": "公式在頁面上是可選取的文字，跟著使用者的字級與深/淺色主題走；長公式在手機上不會撐破版面（外層 overflow-x:auto 的捲動容器，且有捲動陰影提示）。長按/右鍵公式可以「複製 LaTeX」（原始 TeX 存在 data-tex 屬性裡）。KaTeX 字型只在該分頁真的有數學時才載入。"
 },
 {
  "name": "所有預渲染的圖一律用 currentColor / var(--…)，禁止硬寫色碼",
  "rationale": "這站有深/淺色雙主題再疊六種 tone（app.html 的 :root.theme-dark 與 tone-* 兩層），一旦建置期把 #333 烤進 SVG，深色模式下所有函數圖、決策樹、化學結構就會變成黑底黑線。這條規則必須寫進 lint 而不是靠自覺，因為它在淺色模式下完全看不出問題，會一路上線到深色使用者臉上。圖表管線的選擇：Mermaid 執行期要 500KB+ 且版面會抖，否決；建置期跑 Mermaid 需要無頭瀏覽器，太重。流程圖/決策樹走 graphviz dot（subprocess 呼叫，決定性輸出、CI 容易裝）；函數圖與統計圖走自寫的小型 plotter（```plot 區塊裡是 JSON 規格，Python 取樣後直接吐 <path d>），輸出小、可控、可測。表格一律語意化 <table>，外面包 overflow-x:auto 的可聚焦容器（role=region + tabindex=0），手機上才不會橫向撐破 body。",
  "concrete_ui": "同一張拋物線圖在淺色是深藍線配淺灰格線、切到深色自動變成亮青線配深灰格線，不會有第二份檔案；圖底下的圖說固定是「圖 3-2 ｜ …」的編號格式，可被 [[…#fig3-2]] 引用。寬表格在手機上可左右滑，邊緣有漸層陰影暗示還有內容。"
 },
 {
  "name": "分片以「章」為不可變快取單位，成員固定，靠 content.lock.json 保證改一單元只失效一片",
  "rationale": "vercel.json 已經把 /data/(.*) 設成 max-age=31536000, immutable，而 tools/stamp-assets.py 用 sha256 前 10 碼改寫 HTML 裡的 ?v=。這套機制很好，但有個在內容站會爆炸的陷阱：如果分片是「把所有單元按順序切成每 N 個一包」，那新增一個單元會讓後面所有分片的成員位移，於是所有分片的 hash 全變，一次小修訂就讓使用者重新下載整科。所以分片鍵必須是內容自身的穩定屬性（科目/章），且章內成員只增不重排；content.lock.json 記錄 unitId → chunk → contentHash，建置時若偵測到某單元換了 chunk 就明確報錯要求人工確認。data/*.js 沿用既有的兩種格式慣例：內容分片用 ES module（export const UNITS = {...}），配 dynamic import('./data/content/math/ch03.js?v=hash')——這正是 app.html:3932–13713 一直在用的懶載入模式，模組快取會自動去重。",
  "concrete_ui": "使用者在同一章裡跳單元是瞬間的（同一片已在記憶體）；跨章第一次跳有一個約 100–200ms 的骨架屏（沿用 dc-runtime 既有的 .sc-placeholder shimmer 樣式）。站方發佈勘誤修訂後，回訪的使用者只重抓那一章，其他科目完全命中快取。"
 },
 {
  "name": "搜尋：建置期 bigram 倒排索引 ＋ 桶分片，索引端與查詢端共用同一個 tokenizer",
  "rationale": "純前端建索引（MiniSearch/FlexSearch/Lunr）要先把全文下載到瀏覽器，幾十萬字直接出局。中文斷詞是這題真正的難點，兩條路：字典式斷詞（jieba）對「複數平面」「莫耳濃度」「原子軌域」這類學科詞會切壞，而且擴字典是無止境的維護；Intl.Segmenter 雖然瀏覽器已普遍支援，但各引擎 ICU 版本不同、切法不一致——而只要索引端與查詢端的切法有一點分歧，召回率就會無聲地爛掉。所以主力用 bigram（「二次函數」→ 二次/次函/函數），這也是 Lucene CJKBigramFilter 與多數中日韓搜尋引擎的實際做法：不需要字典、對未登錄詞（人名、化學式、專有名詞）天然免疫、索引端與查詢端共用同一個十行函式，不可能分歧。代價是索引約 2 倍大與少量跨詞邊界誤命中，後者用位置資訊做相鄰驗證即可濾掉。索引依 hash(term)%64 分桶，一次查詢只抓 1–8 個桶（每桶壓縮後約 30–80KB）；posting list 用 delta 編碼＋base36 存成字串，JS 解析比 JSON 物件陣列快得多。排名用 BM25（df 存在桶裡）＋欄位加權（標題×5、S01 定位×3、考點碼×3）。誠實的一句：Pagefind 幾乎就是這套東西的現成 Rust 實作（建置期分片索引、零後端、MIT），應該先花一天用 200 題人工查詢集實測它的中文召回率，贏了就直接用，省下數週；自建方案是它 CJK 表現不佳時的備案，而不是預設。",
  "concrete_ui": "⌘K / 手機上的搜尋鍵開啟指令面板（沿用 app.html 既有的 buildCmd 模式）。輸入第一個字就有結果，因為一份約 25KB 的 titles 索引（所有單元標題＋考點碼＋別名）在 idle 時就預載了；桶檔在背景抓，抓到後結果列表就地補上全文命中，並顯示「數學 A ▸ 第 3 章 ▸ S07 例題 4」的路徑麵包屑與高亮片段。同義詞表讓「三角比」也能找到「三角函數」、「mole」能找到「莫耳」。"
 },
 {
  "name": "複習排程：用 FSRS，但同步的是複習事件、不是排程狀態",
  "rationale": "SM-2 只有二十行、好懂，但已被大量實證資料證明在相同保留率下多排約 20–30% 的複習量；FSRS（ts-fsrs，MIT，約 10–20KB）是 Anki 24.11 起的預設，參數只有十幾個浮點數，成本完全可接受。真正的關鍵設計不在選哪個演算法，而在資料結構：把 review log 當唯一真相，stability / difficulty / due 全部由 fold(FSRS, 依 (ts, id) 全序排好的 logs) 推導出來。這一個決定同時解掉三個問題——(1) 換演算法或使用者重新最佳化參數時，歷史不用遷移，重算即可；(2) 跨裝置同步不會有「哪台的 due_at 才對」這種無解衝突，因為 due_at 根本不同步；(3) 兩台裝置離線各答同一張卡，在領域語意上兩筆都算數，用事件集合聯集就結束了，而 row-based LWW 會把其中一筆吃掉。op id 用 UUIDv7（時間排序＋天然去重鍵）。卡片本身（id、所屬單元、srcHash）是建置產物不是使用者資料，卡 id 用 unitId＋序號的穩定雜湊，內容修訂時 id 不變、歷史留存、只在 UI 標「內容已更新」。儲存改用 IndexedDB 而不是 localStorage：複習紀錄會超過 5MB，而 localStorage 是同步 API，寫入會卡主執行緒。",
  "concrete_ui": "複習頁是一張卡一屏，底部四顆評分鍵（重來 / 困難 / 良好 / 容易），每顆上面直接顯示「下次：3 天後」的預估間隔。單元頁旁邊有一顆進度環顯示該單元卡片的到期數。設定裡有「重算排程」按鈕（fold 重跑，秒級完成），以及一句誠實說明：「你的複習紀錄是原始資料，排程隨時可以重算，換演算法不會弄丟任何東西。」"
 },
 {
  "name": "同步策略：每個裝置只寫自己的 key，讓衝突在結構上不可能發生",
  "rationale": "必須貼著既有後端的真實形狀設計，不能空想。worker/src/api.js 的 /api/prefs 是一個 KV：GET 回整包、POST 逐 key upsert（db.js:66 的 ON CONFLICT DO UPDATE），沒有 If-Match、沒有版本號，所以同一個 key 被兩台裝置寫就是後到的無條件覆蓋。硬限制是 MAX_PREFS = 256KB（單次 POST）與 MAX_PREF_KEYS = 200（總 key 數）。順著這個形狀，最乾淨的解是單寫者分割：複習紀錄存成 srs/log/<deviceId>/<段>，一台裝置永遠只 append 自己的 key，於是兩台裝置永遠不會寫同一個 key，寫入衝突在結構上不存在，完全不需要動後端。拉取 = GET 整包 → 聯集所有 srs/log/* → 依 op id 去重 → 依 (ts, id) 排序 → fold。因為 op 以 id 冪等，聯集本身就是正確的合併，不需要任何「保留本機還是保留雲端」的對話框——那個對話框正是使用者實際弄丟資料的地方。容量要算：op 用位置陣列＋卡片字典＋ts delta 編碼壓到約 12–18 bytes，重度使用者一個月約 6000 筆 ≈ 90KB，安全。200 key 的上限用「檢查點＋尾巴」處理：每台裝置把自己 90 天前的已封存段折成一份 srs/ckpt/<deviceId> 快照後刪掉原始段——檢查點也是 per-device 的，所以一樣沒有競爭。真正會衝突的只有設定（srs/settings 這種共享 key），對它用欄位級 HLC LWW（每個欄位存 {v,t}），POST 前先 GET 合併——絕不能用整包 LWW，那會讓一台裝置改主題色就把另一台改的每日上限蓋掉。刪除一律用墓碑，不然會從另一台裝置復活。",
  "concrete_ui": "未登入就完全可用，資料存在本機，設定頁顯示「本機模式 · 1,284 筆複習紀錄」。首次登入時不問任何問題，直接顯示「已合併 1,284 筆本機紀錄與雲端 3,051 筆，去重後共 3,977 筆」。同步狀態是導覽列一顆小圓點：綠＝已同步、黃＝待上傳 N 筆、灰＝離線。設定裡保留 app.html 既有的 BK_KEYS 匯出/匯入機制，擴充成可匯出 NDJSON 的完整 op log。"
 },
 {
  "name": "首屏預算 300KB（br）：內容一律不進 shell，並補上 Service Worker",
  "rationale": "app.html 現在是 1.27MB（294KB 模板 ＋ 770KB 邏輯），而 vercel.json 給所有 *.html 的是 max-age=0, must-revalidate——每次進站都要重新驗證這個大檔。這是這個專案最該引以為戒的地方，也是新站唯一不能重蹈的錯。預算切法：shell HTML（模板＋邏輯，內容零）≤ 120KB br、React UMD 約 45KB br、support.js 約 18KB br、CSS 約 15KB br、目錄 manifest ≤ 30KB br、首屏中文字型 subset ≤ 60KB（unicode-range 分片，常用字先到），合計 ≤ 300KB br，目標 4G 下 LCP < 2.0s、主執行緒 parse+compile < 250ms。內容分片目標是一章 30–80KB br、單元中位數 15–25KB。這站目前完全沒有 Service Worker，而 immutable 的資產 URL 讓 SW 變得非常好寫：shell 與 vendor 預快取、/data/** 因為 URL 帶內容雜湊所以可以 cache-first 永不重驗、HTML 走 stale-while-revalidate。順帶得到離線能力，而離線對「在通勤上讀書」的考生是實質功能不是加分項。",
  "concrete_ui": "設定頁有「離線下載」清單，每科一列顯示大小（例：數學 A · 18.4 MB），可個別勾選下載，有進度條與「已下載，可離線閱讀」狀態。目錄頁的單元連結在 hover / touchstart 時就開始 prefetch 該章分片，所以點下去多半是零等待；讀完一個單元時，「下一單元」會在 idle 時預抓。"
 },
 {
  "name": "品質控管是建置閘門，不是流程建議：lint 不過不能上架，AI 草稿未經真人簽名不進 production 分片",
  "rationale": "「請大家記得標來源」這種規範在幾百個單元的規模下必定失效，唯一有效的是讓 CI 擋下來。tools/lint-content.py 應該直接 fail build：S01–S14 區塊齊全且順序正確、字數落在規格區間、S07 每題七欄位齊全、每個結論必須有 source / derivation / convention 三者之一、prereq 與 next 指向存在的單元、[[…]] 連結目標存在、每條 TeX 能被 KaTeX strict 模式編譯、每張圖有 alt 且沒有硬寫色碼、layer 標記單調。來源標註結構化在 front matter：sources 每筆記 kind/ref/year/url/accessed，歷屆題用 origin: {exam: 學測, year: 113, subject: 數A, number: 12}，年度一律民國西元雙寫避免歧義。AI 產生的解析要三重防線：第一層也是最有價值的一層是機械驗算——AI 最常錯的正是計算與代數化簡，而那正好是可以自動驗的，用 sympy 實際把答案算一次跟 answer 欄位比對，對不上就 fail；第二層是用第二個模型獨立解一次，答案不一致就標 needs_human；第三層是真人學科審查（GitHub CODEOWNERS 按科目分派 PR reviewer，reviewed.subject 欄位記簽名者與日期）。有一條紅線要單獨立起來：絕不讓 AI 生成來源標註欄位——出處只能由人填或由題庫查詢自動帶入，否則會產出看起來極度可信的假引用，那是這整套系統最危險的失效模式。勘誤回報建議先用 GitHub Issue 預填 URL 上線（零後端、公開可追蹤、天然版控），量起來再搬進 Worker（新增 /api/errata 與一張 D1 表，沿用 dashboard.js 既有的後台模式）。",
  "concrete_ui": "每個單元頁底部有一列後設資訊：版本、最後審閱日、審閱者、來源清單（可展開）。未經人工審閱的段落有明顯的黃色 badge「AI 協助草擬，尚未經學科審閱」，而且這種單元根本不會出現在 production 分片裡，只有帶 ?preview=1 才看得到。每個區塊右側有「回報」小按鈕，點下去預填單元 ID、區塊 ID 與內容雜湊。站上有一頁公開的勘誤看板，單元頁顯示「已知勘誤 2 則」。"
 },
 {
  "name": "規模結論：模板大小由「頁面型別數」決定而非頁面數；同時保留同一份源檔也能產靜態頁的退路",
  "rationale": "誠實評估 dc-runtime 在幾十萬字＋上百頁的表現，斷點有四個且都可量測。(1) 模板編譯是啟動時同步的：compileTemplate 對整份模板做一次 DOMParser、對每個元素 setAttribute('data-dc-tpl')、對純 inline 的元素還要 clone 子樹算 contentKey 雜湊，並把 tpl.innerHTML 再存一份到 render.__annotated。app.html 現在 294KB 模板、251 個 sc-if、175 個 sc-for 已在做這件事。若把上百個單元頁各寫成一個 sc-if 分支，模板會線性膨脹到 1–2MB，首屏會出現數百 ms 的 long task。但學科站其實只有約十二種外殼（單元閱讀、題目、卡片複習、目錄、搜尋、統計、設定…），內容全部資料驅動，模板可以收在 60–100KB——所以這條是可控的，前提正是「內容不進模板」。(2) renderVals 每次 render 全跑，且因為 expr.ts 不能做運算，所有東西都得先算好；app.html 的 renderVals 已經約 2400 行。頂層 sc-if 擋得住渲染，但擋不住 renderVals 本身，所以要分區 memo（依相依 state 欄位當 key）＋非當前頁提早 return。這是最實際的效能陷阱。(3) sc-for 一律用陣列 index 當 key，也沒有 sc-else（DECK_CONTROL_FLOW_RE 裡列了 sc-else，但 walk() 根本沒處理它，會被當成一般元素），長清單重排會整段重建 —— 題庫列表必須沿用 app.html 既有的 slice 分頁模式。(4) 單檔 must-revalidate。四個都有解，所以結論是撐得住。但有一個 dc-runtime 解不了的問題必須講：SEO。學測站的自然流量幾乎全靠學生 Google 搜「學測 三角函數 公式」，而把上百個單元藏在 ?page= 後面的 SPA，搜尋引擎幾乎抓不到。所以正解是混合形態——建置腳本從同一份 .unit.md 額外產出 /s/<科目>/<單元>.html 這種已含首屏內容的靜態頁（Vercel static 出上百個 HTML 完全沒問題），站內導覽再用 pushState 接管改抓資料分片，同時拿到 SEO、可分享連結、首屏速度與 SPA 的順滑。至於換 Astro/Next（它的 content collections 幾乎就是這份規格的現成實作）——只在模板超過 150KB 或 renderVals 超過 3000 行時才值得，而最重要的保險是：把建置管線設計成「產出純資料 ＋ 純 HTML」，這樣未來真的要換渲染層，內容遷移成本是零。",
  "concrete_ui": "使用者從 Google 點進 /s/math/quad-01.html，看到的是已經有完整內文的頁面（不是骨架屏）；之後在站內點任何連結都不再整頁重載，網址列照樣變成真實路徑而不是 ?page=。分享出去的任何連結都能直接命中該單元的該區塊。開發端則有 npm run budget 會在 shell 超過 120KB br 或模板超過 100KB 時讓 CI 變紅。"
 }
]

## features
[
 {
  "name": "tools/build-content.py — 內容主管線",
  "why": "整條 .unit.md → data/content/*.js 的 driver：解析 front matter、Markdown 轉 HTML、呼叫數學與圖表子管線、解析並驗證 [[…]] 交叉引用、依 layer 標記標注三視圖、白名單淨化、分片輸出。沒有它其他一切都不存在。",
  "priority": "P0",
  "notes": "沿用 tools/build-tutorial.py 的寫法慣例（ROOT 常數、輸出檔頭寫「由 tools/… 產生，勿手改」、結尾印統計）。Markdown 用 markdown-it-py 加三個自訂外掛：::: L3 容器、[[wiki 連結]]、型別化 fenced 區塊。輸出中間產物 build/units/*.json 供搜尋索引與 lint 共用，避免重複解析。"
 },
 {
  "name": "tools/render-math.mjs — KaTeX 批次預渲染（含雜湊快取）",
  "why": "建置期把所有 $…$ / $$…$$ 轉成 htmlAndMathml，執行期完全不需要數學函式庫。批次是關鍵：整份 stdin JSON 進、stdout JSON 出，node 只啟動一次而不是每條公式一次。",
  "priority": "P0",
  "notes": "以 sha256(tex + display flag) 當 key 存 build/.mathcache.json，重建時只算新增與修改的公式——幾十萬字規模下這決定了「改一個字要等 3 秒還是 3 分鐘」。KaTeX strict 模式，編譯失敗即 build fail 並印出檔名行號。同時把原始 TeX 寫進 data-tex 屬性供「複製 LaTeX」使用。"
 },
 {
  "name": "tools/lint-content.py — 上架閘門",
  "why": "把 docs/research/內容深度/單元講解規格與範例章節.md 的 S01–S14 規格變成可執行的檢查。規範不進 CI 就等於不存在。",
  "priority": "P0",
  "notes": "檢查項：區塊齊全與順序、字數區間、S07 七欄位、每個結論有 source/derivation/convention、prereq/next 與 [[…]] 目標存在、圖片存在且有 alt、SVG 無硬寫色碼、layer 標記單調、YAML 內含 TeX 者必須用區塊純量。輸出 GitHub Actions annotation 格式，直接標在 PR 的行上。"
 },
 {
  "name": "分片器與 content.lock.json",
  "why": "保證「改一個單元只讓一片快取失效」。分片鍵是科目/章、章內成員只增不重排；lock 檔記錄 unitId → chunk → contentHash，偵測到單元換片就報錯要人工確認。",
  "priority": "P0",
  "notes": "同時產出 data/content/manifest.js（目錄樹、每片 hash、字數、版本、審閱狀態）。manifest 是唯一會進首屏預算的內容檔，要壓在 30KB br 以內——所以它只放結構不放摘要。跑完接 tools/stamp-assets.py 打 ?v= 版本戳（該腳本已經涵蓋 data/ 目錄，不需修改）。"
 },
 {
  "name": "tools/build-search-index.py — bigram 倒排索引與桶分片",
  "why": "全站搜尋的核心。索引端與查詢端共用同一個十行 tokenizer，這是中文召回率不會無聲爛掉的唯一保證。",
  "priority": "P0",
  "notes": "hash(term)%64 分桶、posting list 用 delta + base36 字串、df 存在桶內供 BM25。另外產出永遠預載的 data/search/titles.js（≤25KB br）給 ⌘K 的即時第一屏。開工前先花一天用 200 題人工查詢集實測 Pagefind 的中文召回率——贏了就直接用它，這個腳本就不用寫。"
 },
 {
  "name": "js/content-widgets.js — 內容用 Custom Elements",
  "why": "注入的 HTML 字串沒有 React 節點，但瀏覽器會在 innerHTML 解析時自動升級自訂元素，所以互動性可以完全靠它們達成，不需要注入後掃 DOM 綁事件。",
  "priority": "P0",
  "notes": "<ex-answer>（遮解答）、<ex-fold>（深挖段落摺疊）、<ex-tree>（可點決策樹）、<ex-card>（內嵌速記卡，點一下加入複習）、<ex-report>（區塊勘誤回報）。狀態放元件自身，絕不觸發外層 setState——否則會整頁 re-render 並重跑 renderVals。用 helmet 內的 <script src> 掛載，避開 x-import 的 new Function 以保留未來上 script-src 'self' 的可能。"
 },
 {
  "name": "js/srs.js — FSRS 排程核心與 IndexedDB op-log",
  "why": "複習功能的心臟。append-only 的 review log 是唯一真相，排程狀態一律 fold 出來。",
  "priority": "P0",
  "notes": "ts-fsrs（MIT）自架在 /vendor。op 結構 [uuidv7, cardId, ts, rating, durMs]；IndexedDB 兩個 store：ops（keyPath=id，uuidv7 天然時序）與 derived（fold 快取，key 是輸入集合的雜湊）。fold 是純函式且必須可單獨測試——它決定所有裝置會不會收斂到同一個 due。卡片本身來自建置產物 data/cards/*.js，卡 id = hash(unitId + 序號)，內容修訂不換 id。"
 },
 {
  "name": "js/sync.js — D1 prefs 同步層（單寫者分割 ＋ 檢查點）",
  "why": "把 op-log 塞進一個沒有版本控制的 KV 端點而不遺失資料。每台裝置只寫 srs/log/<deviceId>/<段>，寫入衝突在結構上不存在，且完全不必改 worker。",
  "priority": "P0",
  "notes": "必守既有硬限制：MAX_PREFS 256KB（worker/src/api.js:33）與 MAX_PREF_KEYS 200（:35）。op 壓縮到 12–18 bytes（位置陣列 + 卡片字典 + ts delta）。90 天前的段折成 per-device 的 srs/ckpt/<deviceId> 快照後刪原始段，控制 key 數。共享 key（srs/settings）用欄位級 HLC LWW，POST 前先 GET 合併；刪除一律墓碑。首次登入直接聯集去重，不出現「保留本機還是雲端」的對話框。"
 },
 {
  "name": "tools/verify-answers.py — 例題答案機械驗算",
  "why": "AI 產生解析最常錯的就是計算與代數化簡，而那正好可以自動驗。這是投報率最高的一道防線。",
  "priority": "P1",
  "notes": "用 sympy 對可符號化的題目（多項式、方程、微積分、極限、機率算式、單位換算、化學平衡係數）實際算一次，與 answer 欄位比對，不一致就 fail build 並列出差異。無法符號化的題型標記 unverifiable 並強制走真人審查路徑。"
 },
 {
  "name": "tools/build-figures.py — 圖表管線（plot / tree DSL → 主題化 SVG）",
  "why": "函數圖、統計圖、決策樹、知識圖譜邊要能從純文字規格產生，才可 diff、可 review、可批次改版。",
  "priority": "P1",
  "notes": "```plot 區塊是 JSON 規格，Python 取樣後直接吐 <path d>；流程圖與決策樹走 graphviz dot subprocess（決定性輸出）。所有 stroke/fill 一律 currentColor 或 var(--accent) 等 token，由 lint 強制。輸出 inline SVG（不是 <img>），才能繼承主題與字級。"
 },
 {
  "name": "Service Worker ＋ 離線科目包",
  "why": "站上目前完全沒有 SW。immutable 的內容 URL 讓 cache-first 變得完全安全，順帶把「通勤時離線讀書」變成實質功能。",
  "priority": "P1",
  "notes": "shell 與 vendor 預快取、/data/** cache-first 永不重驗（URL 已帶內容雜湊）、HTML stale-while-revalidate。離線包以「章」為單位 addAll，UI 顯示每科 MB 數與進度。注意 Safari 不支援 Background Sync，同步只能在前景做，不要在 UI 上承諾背景同步。"
 },
 {
  "name": "tools/build-images.py — 圖片管線",
  "why": "掃描圖與照片要壓成 AVIF/WebP 多尺寸，並把實際尺寸寫回 HTML 的 width/height 以消除 CLS。",
  "priority": "P1",
  "notes": "沿用既有 tut-img/ 的 webp 慣例與 build-tutorial.py 裡的 <img loading=\"lazy\" style=\"max-width:100%;height:auto\"> 輸出格式，補上 srcset 與明確 width/height。同時檢查授權欄位：自製 SVG / CC（必須輸出 BY 標註）/ 需授權（直接 fail，不准上線）。"
 },
 {
  "name": "勘誤回報：GitHub Issue 預填 →（後續）/api/errata",
  "why": "回報要帶單元 ID、區塊 ID 與內容雜湊，才能判斷該回報針對的是不是舊版內容。",
  "priority": "P1",
  "notes": "第一階段用預填的 GitHub Issue URL：零後端、公開可追蹤、天然版控。量起來再搬進 worker（新增 /api/errata 與一張 D1 表，沿用 worker/src/dashboard.js 既有的後台列表模式）。SLA 分兩級：影響答案正確性的 24 小時內修並發 hotfix 分片，錯字排週更。"
 },
 {
  "name": "tools/build-static-pages.py — SEO 靜態頁產生器",
  "why": "學測站的自然流量幾乎全靠搜尋引擎，而 SPA 的 ?page= 路由抓不到。從同一份 .unit.md 額外產出 /s/<科目>/<單元>.html，內容零額外成本。",
  "priority": "P1",
  "notes": "每頁含首屏內容、正確的 <title>/description/canonical、JSON-LD（Article + 麵包屑），並掛同一份 shell script 讓站內導覽用 pushState 接管。這同時是「未來換 Astro/Next」的保險：渲染層可換，內容遷移成本為零。上百個靜態檔對 Vercel static 沒有壓力，vercel.json 只需在現有 routes 前加一條 /s/(.*) 規則。"
 },
 {
  "name": "內容預覽環境（?preview=1 ＋ data/content-preview/）",
  "why": "未經真人學科審查的單元（含所有 AI 草稿）必須物理上進不了 production 分片，而不是靠 UI 藏起來。",
  "priority": "P1",
  "notes": "lint 依 reviewed.subject 是否有簽名決定單元進哪個目錄。預覽站帶 noindex，並在每頁頂端有一條顯眼的橫幅。審查者在 GitHub PR 上看 diff、在預覽站上看成品，兩邊都要能連到同一個單元 ID。"
 },
 {
  "name": "字型 subset 工具",
  "why": "中文字型是首屏預算的最大單項。Huninn 全字集遠超過 60KB 的預算，KaTeX 也有十幾個字重多數用不到。",
  "priority": "P2",
  "notes": "用 fonttools 依全站實際用字產生 unicode-range 分片（常用 3500 字一片先載，罕用字後補），KaTeX 只保留實際出現過的 face。全部自架在 /vendor 以支援離線並拿掉對 fonts.googleapis.com 的依賴（那是目前 helmet 裡唯一的外部 CSS 來源，也是未來立 CSP 的障礙）。"
 },
 {
  "name": "作者本機預覽伺服器（watch 模式）",
  "why": "寫作循環的速度決定內容產出量。存檔後兩秒內看到成品，才可能讓學科老師願意直接寫 Markdown。",
  "priority": "P2",
  "notes": "watch content/**/*.unit.md，增量只重跑改動的單元（數學快取讓這件事變便宜），跑完 websocket 通知瀏覽器重載該分片。同時即時顯示 lint 錯誤清單與該單元的三視圖字數統計。"
 },
 {
  "name": "內容健康度儀表板",
  "why": "上百個單元、多人協作、加上 AI 草稿與勘誤，沒有一個總覽就無法管理進度與品質債。",
  "priority": "P2",
  "notes": "由建置產物直接算出：各科覆蓋率、審閱進度（未審 / 已學科審 / 已教學審 / 已編輯審）、AI 草稿比例、勘誤未結案數與平均處理時間、每單元字數與三視圖時長是否落在規格區間、知識圖譜的孤兒節點與斷鏈。輸出成一頁靜態 HTML 就夠，不需要後端。"
 }
]

## risks
[
 "把內容寫進模板（在 <x-dc> 裡為每個單元開一個 sc-if 分支）——這是唯一會真正壓垮 dc-runtime 的做法。模板編譯是啟動時同步且 O(模板大小)：完整 DOMParser、逐元素 setAttribute('data-dc-tpl')、inline 元素還要 clone 子樹算 contentKey 雜湊，並額外保留一份 tpl.innerHTML。app.html 的 294KB 模板已在做這件事；漲到 1–2MB 就是首屏數百 ms 的 long task。防線是把「模板大小 ≤ 100KB」寫成 CI 預算檢查，而不是靠自律。",
 "renderVals 每次 render 全跑。expr.ts 不能做算術也不能呼叫函式，所以所有東西都得先算好；app.html 的 renderVals 已約 2400 行，而頂層 sc-if 只擋渲染、擋不住 renderVals 本身。搜尋框每打一個字就把全站所有頁的資料重算一次，是這套架構最容易踩到又最難察覺的效能陷阱。必須從第一天就分區 memo 並對非當前頁提早 return。",
 "注入內容的 React element 沒有記憶化的話，每次 setState 都會重新 parse 一次 20KB 的 HTML 字串。這在開發時（內容短）完全看不出來，會等到內容量上來才爆。this._elCache 以 unitId+contentHash 為 key 必須是硬性規範。",
 "分片成員不穩定會讓 immutable 快取失去意義：若分片是「按順序每 N 個一包」，新增一個單元就讓後續所有分片的 hash 全變，一次小修訂等於要使用者重下整科。content.lock.json 的換片偵測是唯一防線。",
 "讓 AI 產生來源標註或年度出處。這會生出看起來極度可信的假引用，而且幾乎無法靠抽查發現——比錯誤的解題步驟危險得多，因為錯的計算學生會算不出來，錯的出處沒有人會去查。出處欄位只能由人填或由題庫查詢自動帶入，必須是不可協商的紅線。",
 "中文斷詞在索引端與查詢端不一致。若建置期用 jieba 而瀏覽器端用 Intl.Segmenter（各引擎 ICU 版本不同、切法不同），召回率會無聲地爛掉，而且測試時因為常見詞都切得對，很難發現。共用同一個 bigram tokenizer 是唯一結構性的保證。",
 "預渲染 SVG 硬寫色碼。淺色模式下完全正常，深色使用者會看到黑底黑線。必須靠 lint 擋，因為它在開發者的機器上百分之百看不出來。",
 "D1 prefs 的硬限制被忽略：MAX_PREFS 256KB 單次、MAX_PREF_KEYS 200 個 key 總量，而且端點沒有版本控制（POST 直接覆蓋）。重度使用者兩三年的複習紀錄若不做壓縮與檢查點折疊，會撞上限並開始靜默失敗；共享 key 若用整包 LWW，會出現「在手機改了每日上限，電腦一同步就被蓋回去」這種難以重現的資料遺失。",
 "SEO 幾乎必然被低估。上百個單元藏在 SPA 的 ?page= 後面，而學測站的自然流量本質上就是搜尋流量。等到內容都寫完才發現搜不到，靜態頁產生器就得回頭重做整個路由與連結結構——這是要在第一週決定、不是第六個月決定的事。",
 "localStorage 沿用慣性。app.html 現在全站用 sekai-* 前綴的 localStorage，但複習紀錄會超過 5MB 且 localStorage 是同步 API，寫入會卡主執行緒。順手沿用既有模式是很自然的選擇，也是後來最痛的一次資料遷移。",
 "dc-runtime 的隱形限制：沒有 sc-else（DECK_CONTROL_FLOW_RE 裡列了但 walk() 根本沒實作，會被當一般元素靜默渲染出來）、sc-for 一律用陣列 index 當 key（長清單重排會整段重建）、x-import 用 new Function（未來要立 CSP 就得開 unsafe-eval，好消息是 app.html 目前一次都沒用到，這條路還沒被堵死）。這些在文件裡查不到，只能從 support.js 讀出來，新加入的人一定會踩。",
 "規格本身的執行風險：docs 的 S01–S14 規格對每個單元的要求非常高（L2 讀完 25–40 分鐘、S07 每題七欄位齊全）。上百個單元乘上這個密度是很大的工程量，若沒有先用一個完整單元跑通「寫作 → lint → 審查 → 上架」的全流程並實測單人工時，排程會嚴重失準，最後的壓力會轉嫁成「放寬 lint」——那等於整套品質控管失效。"
]
