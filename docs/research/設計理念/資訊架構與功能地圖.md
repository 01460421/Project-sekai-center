
## features
[
 {
  "name": "三層路徑路由器（pathname 版 dc-runtime 路由）",
  "why": "模板現在只有 ?page= 一層，子分頁（ctab）完全無法深連結，站內 AI 工具的說明甚至要向使用者道歉「網址無法直接指定」。學測站的最小分享單位是「節」，沒有三層路由整個產品就不成立。",
  "priority": "P0",
  "notes": "改寫 componentDidMount 的起始頁邏輯：解析 location.pathname 而非 URLSearchParams；每次切頁 history.pushState + 監聽 popstate；vercel.json 已經是 /(.*) → app.html 的 catch-all，不必改 rewrite 規則，只要把 immutable 資產路徑排在前面。保留 ?page=xxx 舊網址 → 新路徑的對照表做 client-side 301。"
 },
 {
  "name": "章節內容分片資料層 data/{subject}/{chapter}.js",
  "why": "單檔 HTML 是模板的優點也是天花板——app.html 已經 1.27MB。內容站的量級是它的十倍，只能靠動態 import 分片，讓 HTML 只裝殼與路由。",
  "priority": "P0",
  "notes": "沿用既有的 import('./data/xxx.js?v=hash') + tools/stamp-assets.py 內容雜湊戳記與一年 immutable 快取。每章一檔，超過 150KB 再切節。載入中要有骨架（模板 sc-for 的 hint-placeholder-count 已有這個概念）。"
 },
 {
  "name": "科目目錄索引 data/index/{subject}.js",
  "why": "側欄目錄樹、麵包屑、上/下一節、搜尋標題層全部只需要標題與 id，不需要正文。把它獨立成小檔，才能做到「進科目首頁 200ms 內看到完整目錄」。",
  "priority": "P0",
  "notes": "只含 id／標題／層級／預估分鐘／卡數／題數／難度，單科控制在 20KB 內。"
 },
 {
  "name": "六科主題色系統（沿用 UNITS 色票）",
  "why": "模板 UNITS 的六個團體色已經是調校過、深淺主題都能用的一組色，直接對應六科可以省掉整輪視覺設計，也讓使用者三天內建立「藍＝國文、紫＝社會」的顏色記憶。",
  "priority": "P0",
  "notes": "ln #4455dd→國文、vs #44cccc→英文、mmj #88cc44→數A、wxs #ffbb33→數B、n25 #aa66cc→社會、vbs #ee6644→自然。四個子科用同色相的明度階，不另開色相。"
 },
 {
  "name": "麵包屑＋上/下一節導覽",
  "why": "深連結進站的人（搜尋流量的主要入口）落在第三層，沒有麵包屑就不知道自己在哪、也不會往上逛。",
  "priority": "P0",
  "notes": "麵包屑每層可點；上/下一節按鈕吃 data/index 的線性序，鍵盤 J/K 可跳。"
 },
 {
  "name": "cmdk 全站搜尋（三層索引）",
  "why": "六科上千節內容，選單導覽必然失敗，搜尋才是主導覽。模板已有 cmdk 狀態與命令面板骨架，改成內容搜尋是最短路徑。",
  "priority": "P0",
  "notes": "第一層標題索引背景載入、第二層該科全文倒排索引進科目才載、第三層 Worker /api/search（D1 FTS5）兜底。中文用 bigram 切分，不引入斷詞器。"
 },
 {
  "name": "最近閱讀與「繼續上次」",
  "why": "每天回訪的學生第一個動作是「我昨天讀到哪」。沒有這個按鈕，每天都要重新導覽一次，回訪成本高到會流失。",
  "priority": "P0",
  "notes": "localStorage 環形佇列存 20 筆（id／標題／捲動百分比／時間），登入後同步到 D1。首頁與科目首頁各放一個大按鈕。"
 },
 {
  "name": "章節閱讀頁（doc-shell 形態）",
  "why": "這是全站流量最大、SEO 價值最高的頁面形態，其他一切都掛在它上面。",
  "priority": "P0",
  "notes": "版面：科目色條＋麵包屑＋正文＋右側章節內小目錄＋頁尾三個掛件（本節複習卡 N 張／本節考古題 M 題／加入計畫）。"
 },
 {
  "name": "複習卡系統（SM-2 間隔重複）",
  "why": "讀完就忘是學測複習的核心痛點；間隔重複是唯一有大量實證的解法，也是讓學生每天回站的引擎。",
  "priority": "P0",
  "notes": "卡片綁 section id。站方預設卡免登入可翻看，排程狀態存本機；登入後才跨裝置。到期卡數字要出現在導覽列徽章。"
 },
 {
  "name": "錯題本（自動歸因到章節）",
  "why": "錯題只記「錯了」沒有用，要能回答「我為什麼錯、該回去讀哪一節」。自動歸因是這個產品相對紙本錯題本唯一真正的優勢。",
  "priority": "P0",
  "notes": "錯題＝user×question 的記錄，存選項、原因標籤（觀念／計算／看錯題／時間不夠）、是否已克服。透過題目的 concept tag 反查 section，直接給「回去讀這節」與「產生 3 張補救卡」兩個按鈕。"
 },
 {
  "name": "考古題索引（年度×考科×題號）",
  "why": "「歷屆試題」是學測關鍵字搜尋量最大的一塊，也是把內容站和練習閉環接起來的天然素材。",
  "priority": "P0",
  "notes": "/past-papers/{year}/{subject}/{no} 為單題 canonical，/q/{qid} 短連結 301 過去。詳解必須自製，不能沿用他站。"
 },
 {
  "name": "匿名寫入 → 登入合併",
  "why": "「讀內容永不登入」若沒有這條，第一次來的人一答題就被登入牆擋住，前面的免登入承諾等於白做。",
  "priority": "P0",
  "notes": "所有個人狀態先寫 localStorage（模板收集率頁的勾選就是這個模式），登入時一次性 merge upload，衝突以時間戳較新者為準並保留兩份供人工檢視。"
 },
 {
  "name": "Service Worker 離線快取",
  "why": "通勤與晚自習是主要使用時段，其中一段常常沒有穩定網路。內容站離線可讀是體感差距最大的一項。",
  "priority": "P0",
  "notes": "app shell 與六科目錄 precache；最近讀過 30 節與所有到期複習卡背景 precache；immutable 資產 cache-first、HTML network-first、章節資料 stale-while-revalidate。要有「有新版本，重新整理」的提示條。"
 },
 {
  "name": "倒數計時器",
  "why": "最低成本、最高分享率的鉤子；也是首頁把「今天該做什麼」框起來的框。",
  "priority": "P0",
  "notes": "沿用模板「倒數直接寫 DOM 不 setState」的做法——每秒重跑整個 renderVals 是模板踩過的效能坑。"
 },
 {
  "name": "級分換算器",
  "why": "純靜態資料＋純前端計算，免登入、可分享、每年查詢量集中爆發，是拉新的高效入口。",
  "priority": "P0",
  "notes": "每年的原始分→級分對照表存 data/exam/grade-{year}.js，頁面顯著標示資料年度與來源連結。"
 },
 {
  "name": "制度指南（申請時程、級分制、學習歷程）",
  "why": "考生與家長對制度的焦慮不亞於學科；這類頁面 SEO 長尾強、內容量小、維護成本低，CP 值最高。",
  "priority": "P0",
  "notes": "每頁標「最後更新日期」與大考中心／招聯會原文連結，避免變成過期資訊的來源。"
 },
 {
  "name": "讀書計畫（時間軸＋自動排程）",
  "why": "把散落的章節、複習卡、考古題收斂成「今天做這三件事」，是讓工具從查詢站變成日常工具的關鍵。",
  "priority": "P1",
  "notes": "計畫項目指向 section／card deck／past paper set。要能倒推：輸入考試日與每天可用時數，自動鋪滿。考前兩週自動切換衝刺模式（只排弱點與高頻考點）。"
 },
 {
  "name": "個人儀表板 /me",
  "why": "登入的唯一報酬要看得見：今日任務、到期卡數、六科進度環、弱點雷達。",
  "priority": "P1",
  "notes": "用模板 dashboard.js 的資料聚合模式；六科進度環直接吃科目色。"
 },
 {
  "name": "模考成績追蹤",
  "why": "單次成績沒有意義，趨勢才有。也是落點試算的輸入來源。",
  "priority": "P1",
  "notes": "存成績需登入（屬敏感資料）。輸入後自動換算級分、對照歷年落點、標出退步最多的科目。"
 },
 {
  "name": "落點試算",
  "why": "高關注度功能，但單靠它留不住人；價值在於把它接回「所以你該補哪科」的內容閉環。",
  "priority": "P1",
  "notes": "單次試算免登入；存志願清單、追蹤名額變化才要登入。介面必須強制標示「僅供參考、依據 {年度} 資料」，不能給精確錄取機率。"
 },
 {
  "name": "AI 助教",
  "why": "學生真正的問題是「這題為什麼選 C」而不是關鍵字查詢；模板的 Worker 已有完整 AI 助手與工具呼叫架構，可直接移植。",
  "priority": "P1",
  "notes": "必須綁章節脈絡（把當前 section 內容塞進 context）並在回答末端附「出處：某科某章某節」連結，降低幻覺。免登入每日 N 次（IP 限流），保存對話與追問要登入。"
 },
 {
  "name": "通知與寄信（Worker cron）",
  "why": "複習卡到期、計畫落後、報名截止提醒——把使用者拉回來的唯一主動管道。模板已有 cron 每分鐘與 mail.js。",
  "priority": "P1",
  "notes": "預設全關，逐項 opt-in。考生族群對推播容忍度低，寧可少寄。"
 },
 {
  "name": "跨科概念頁 /topic/{concept}",
  "why": "「機率」在數A、社會（公民統計）、自然（遺傳）都出現；概念頁把它們串起來，同時是大量 SEO 長尾的落點。",
  "priority": "P2",
  "notes": "由 section 與 question 的 concept tag 自動生成，不需人工撰稿。"
 },
 {
  "name": "社群討論",
  "why": "有價值但也是最大的維運負擔（未成年、洗版、代寫答案），應該等內容與工具站穩再開。",
  "priority": "P2",
  "notes": "先只開「章節底下的提問」這一種形態，綁在 section 上，不做自由發文的看板。需登入才發言、免登入可讀。"
 },
 {
  "name": "自製複習卡與牌組分享",
  "why": "UGC 能顯著擴充內容量，但要在站方卡片品質建立標準之後才放開，否則會拉低整體品質印象。",
  "priority": "P2",
  "notes": "分享用短碼；沿用模板既有的分享碼慣例（格式一旦公布就不能改）。"
 },
 {
  "name": "資料來源與更新日誌頁",
  "why": "教育內容站的可信度來自可稽核。學生、家長、老師都會查你的資料哪來的。",
  "priority": "P2",
  "notes": "逐項列出資料來源、抓取日期、授權狀態；自動由各 data 檔的 meta 欄位生成，不手寫。"
 }
]

## principles
[
 {
  "name": "路由必須有三層，且每一層都是真實路徑",
  "rationale": "現行模板只有 ?page=xxx 一層，子分頁（ctab）完全無法用網址指定——app.html 裡站內 AI 的工具說明甚至寫著「子分頁是進頁之後上方那排膠囊按鈕，網址無法直接指定」。學測站的分享單位是「某科某章某節」，第一層路由等於直接廢掉分享、書籤與 SEO。所幸 vercel.json 的最後一條規則已經是 /(.*) → app.html 的 catch-all，任意乾淨路徑都能落到 SPA，缺的只是前端解析 pathname。",
  "concrete_ui": "URL 全表。科目首頁：/chinese /english /math-a /math-b /social /science。社會與自然的子科不另開層級，改成科目層 slug，讓路由永遠是三段、不產生歧義：/social-history /social-geography /social-civics /science-physics /science-chemistry /science-biology /science-earth（/social 與 /science 保留為總覽 hub）。章：/math-a/polynomials。節：/math-a/polynomials/remainder-theorem。節內例題錨點：#ex-3（可分享）。檢視模式走 query、不影響 canonical：?view=cards ?view=quiz。考古題：/past-papers、/past-papers/113、/past-papers/113/math-a、單題 /past-papers/113/math-a/7（canonical），短連結 /q/113-math-a-07 → 301。跨科工具：/tools/grade-converter /tools/admission-simulator /tools/countdown /tools/study-plan /tools/mistakes /tools/flashcards /tools/mock-scores。制度指南：/guide、/guide/timeline /guide/grade-system /guide/portfolio /guide/interview。AI 助教：/ai、/ai/c/{threadId}。個人：/me、/me/progress /me/plan /me/mistakes /me/cards /me/scores /me/settings。社群：/community、/community/{subject}、/community/t/{id}-{slug}。關於：/about /about/sources /about/changelog /about/contribute。搜尋：/search?q=&subject=&type=。概念頁：/topic/{concept}。保留字清單（不得當科目 slug）：tools guide ai me community about search q past-papers topic api data assets。實作面：componentDidMount 改讀 location.pathname，切頁時 history.pushState，並監聽 popstate；?page=xxx 舊網址進站時查對照表做 client-side 轉址。"
 },
 {
  "name": "完整站點地圖：六科內容為主幹，工具掛在內容旁邊",
  "rationale": "多數複習網站把工具（換算、落點）做成獨立孤島，流量進得來卻導不到內容，也導不回。這裡的原則是：任何工具都必須有一條回到章節的路，任何章節底下都掛得到工具。",
  "concrete_ui": "首頁（倒數＋繼續上次＋今日任務＋六科入口卡）→ 六科首頁（該科目錄樹、進度環、弱點提示、該科考古題入口、該科複習卡入口）→ 章首頁（本章各節列表、本章卡數/題數、一鍵加入計畫）→ 節頁（正文＋右側小目錄＋頁尾三掛件：本節複習卡 N 張／本節考古題 M 題／加入計畫）。跨科工具區：級分換算、落點試算、倒數、讀書計畫、錯題本、複習卡、模考成績追蹤、考古題索引。制度指南區：申請入學時程、級分與篩選倍率、學習歷程檔案、面試準備。AI 助教（獨立頁＋每節頁右下角的浮動提問，帶著該節脈絡）。個人儀表板 /me 的六個分頁。社群（先只做掛在 section 底下的提問，不做自由看板）。關於／資料來源／更新日誌／貢獻指南。"
 },
 {
  "name": "六科視覺區辨直接沿用模板的六團色票",
  "rationale": "模板 UNITS 已經有六組在深淺主題下都驗證過的色與對比前景色（app.html 3502-3509 行），色相分得夠開，正好對應六科。省掉整輪配色設計，也讓使用者在幾天內建立顏色記憶：看到紫色就知道自己在社會科的地盤。",
  "concrete_ui": "ln #4455dd→國文、vs #44cccc→英文、mmj #88cc44→數A、wxs #ffbb33→數B、n25 #aa66cc→社會、vbs #ee6644→自然。用法：科目首頁與其下所有章節頁的頂部 4px 色條、麵包屑第一段的色點、側欄目錄的選中底色（color-mix 13% 透明，與模板 navGroups 一致）、進度環與圖表的序列色。社會/自然的四個子科用同色相的明度階（L 各差 8%），不另開色相，避免第七、第八個顏色稀釋記憶。無障礙：色相絕不是唯一區辨——每科同時有固定圖示與文字標籤，且所有色塊上的文字都用 UNITS 裡既有的 t 欄位（例如 wxs 的 #2b2208）確保對比。"
 },
 {
  "name": "兩種頁面形態就夠：doc-shell 與 tool-panel",
  "rationale": "模板實際上已經演化出這兩種：app.html 內的原生頁（輕、共用狀態）與 index.html?embed=xxx 的 iframe 面板（重、獨立、載入自己的資料）。這個分界在內容站更重要——章節頁必須輕到能秒開與被搜尋引擎理解，工具頁可以重且晚載。",
  "concrete_ui": "doc-shell（章節、指南、概念頁、考古題單題）：只吃 data/index 的目錄與該章分片，不載任何計算引擎，首屏 JS 預算 < 80KB。tool-panel（落點試算、級分換算、模考追蹤、複習卡引擎、B30 式的圖卡輸出）：沿用 index.html?embed=xxx 的 iframe 模式或動態 import 的重模組，只有進到該路由才載。兩者共用同一層外框（側欄、麵包屑、搜尋、主題），使用者感受不到邊界。"
 },
 {
  "name": "讀內容永遠不用登入，寫個人狀態才登入，而且是「先用了再說」",
  "rationale": "考生第一次來是從搜尋掉進某一節，登入牆會直接殺掉這條最大的流量。反過來說，同步、通知、成績這類東西沒有帳號本來就做不到。關鍵在中間那段：不要在第一次答錯題時就要求登入，而是先把資料寫在本機，等使用者自己覺得需要跨裝置時再綁定——模板的收集率頁勾選就是本機儲存，只差一個合併上傳。",
  "concrete_ui": "免登入可用：全部章節正文、複習卡翻看、考古題與詳解、全站搜尋、制度指南、級分換算、倒數、單次落點試算、AI 助教每日 N 次（依 IP 限流）。此時所有個人狀態（閱讀進度、卡片排程、錯題、暫存計畫）寫 localStorage。需登入：跨裝置同步、模考成績歷史、AI 對話保存與追問、通知與寄信、社群發言、資料匯出。登入時跑一次 merge upload，把本機資料整批帶上雲、衝突取時間戳較新者。介面上絕不出現「登入才能看」的遮罩，只在頁尾出現一行低調提示：「已在這台裝置記錄 12 題錯題，登入即可在手機上接著看」。"
 },
 {
  "name": "五種核心物件的關係就是一個閉環：讀 → 測 → 錯 → 卡 → 排 → 再測",
  "rationale": "這五種物件如果各自為政，就只是五個互不相干的小工具。真正的產品價值在於它們的外鍵：錯題能自動指回該讀哪一節、能自動長出複習卡、能自動排進明天的計畫。這條鏈是這個站相對紙本錯題本與市面 App 唯一站得住的差異。",
  "concrete_ui": "Section（站方內容，最小內容單位，id 形如 math-a.polynomials.remainder-theorem）— 1:N → Card（複習卡，站方預設卡＋使用者自製卡，含 front/back/難度/tags 與 SM-2 排程欄位）。Section — M:N（透過 concept tag）→ Question（題目：年度、考科、題號、選項、答案、詳解、難度、concept tags）。Question — 1:N（per user）→ Mistake（錯題：作答時間、選了什麼、原因標籤 觀念/計算/看錯題/時間不夠、是否已克服）。Mistake — 自動生成 → 補救 Card ＋ Plan item。Plan（讀書計畫）的每個 item 指向 Section／Card deck／Question set 之一，含日期、預估分鐘、完成狀態。介面上這條鏈要看得見：錯題卡片下方永遠有三顆按鈕——「回去讀 餘式定理」「產生 3 張補救卡」「排進明天」。"
 },
 {
  "name": "學生的三段旅程要各自有一個明確的主畫面",
  "rationale": "第一次來的人要的是答案，每天用的人要的是今天做什麼，考前一週的人要的是別再讀新東西、只補洞。同一個首頁餵這三種人一定失敗，所以首頁要依狀態換臉。",
  "concrete_ui": "第一次來（多半從搜尋落在第三層節頁）：讀完 → 頁尾看到「本節有 12 張複習卡、8 題考古題」→ 試答 → 答錯自動存本機 → 側欄看到全科目錄，往上逛到科目首頁 → 首頁倒數與級分換算勾起動機 → 決定登入。每天用（首頁換成任務臉）：今日計畫 3 項＋到期複習卡 24 張＋繼續上次的按鈕；動線是先刷卡（5 分鐘）→ 讀今日章節（25 分鐘）→ 隨堂 5 題 → 錯題自動入庫 → 收工看六科進度環。考前一週（首頁換成衝刺臉）：計畫自動切衝刺模式，不再排新章節，只排「錯過且未克服」與高頻考點；主畫面是六科弱點雷達＋模考趨勢＋落點校準；倒數改成以天為單位的大字，並附考場、准考證、應考物品的檢查清單。"
 },
 {
  "name": "內容分片：一章一檔，索引與正文分離",
  "rationale": "單檔 HTML 是模板的核心優點，但 app.html 已經 1.27MB；學測六科的內容量是它的數倍，硬塞會讓首屏直接崩掉。模板其實已經給了正解——data/*.js 用動態 import 載入、配 tools/stamp-assets.py 的內容雜湊與一年 immutable 快取。照抄這套即可，只是要把切分粒度想清楚。",
  "concrete_ui": "data/index/{subject}.js：只有標題、id、層級、預估分鐘、卡數、題數、難度，單科壓在 20KB 內，進科目首頁就載，供目錄樹、麵包屑、上/下一節共用。data/{subject}/{chapter}.js：一章正文，30–120KB，超過 150KB 再往下切節。data/exam/{year}-{subject}.js：一份考古題。全部走 import('./data/...js?v={hash}')，改檔後務必跑 tools/stamp-assets.py 重戳版本號——否則會出現 README 警告的「新 HTML 配舊 JS」錯配。預取策略：目錄項目 hover 200ms 或進入視窗時預取該章分片；節頁載入完成後背景預取下一節。"
 },
 {
  "name": "搜尋索引分三層，中文用 bigram 不用斷詞器",
  "rationale": "全站上千節內容不可能靠選單導覽，搜尋就是主導覽。但把全文索引一次送到瀏覽器會是好幾 MB。分層是唯一解：常用的做本機、精確的丟後端。中文斷詞器（jieba 之流）體積大又對教科書術語表現不佳，bigram 索引體積可控且對「餘式定理」「等差數列」這類固定詞組命中率反而更高。",
  "concrete_ui": "第一層 data/search/titles.js：全站標題＋別名＋concept tag，目標 gzip < 150KB，開站背景載入，供 cmdk（模板已有 cmdk/cmdq/cmdi 狀態）做即時前綴與模糊比對，鍵盤 Ctrl/⌘K 開啟。第二層 data/search/{subject}.idx.js：該科正文的 bigram 倒排索引，進到該科任一頁時才載，支援離線全文。第三層 Worker /api/search：D1 FTS5，處理跨科全文與拼錯容錯，也是離線時唯一缺席的那層。搜尋結果一律標科目色點與麵包屑路徑，讓人一眼看出這條結果屬於哪一科哪一章。此外沿用模板 ALIASES 的做法——把「餘式定理｜餘數定理｜remainder theorem」這種別名表寫在資料裡而不是塞進 AI prompt，五百多條每次請求都送一遍太貴。"
 },
 {
  "name": "離線快取以「今天要用的東西」為單位，不是以檔案為單位",
  "rationale": "通勤與晚自習是主要使用時段，其中一段常常沒網路。但把整站 precache 既不現實也沒必要——學生今天真正會碰的，是計畫上的三節、到期的複習卡、和最近讀過的東西。",
  "concrete_ui": "Service Worker 策略：app shell 與六科目錄 precache；「今日計畫涉及的章節分片＋所有到期複習卡＋最近讀過 30 節」在有網路時背景 precache；immutable 資產（data/ vendor/ css/ js/ 帶 ?v= 的）cache-first、永不重驗；HTML network-first（模板 vercel.json 對 HTML 就是 max-age=0 must-revalidate）；其他章節分片 stale-while-revalidate。離線時全站可讀、可刷卡、可作答、可記錯題，只有 AI 助教、社群、成績同步顯示離線徽章。更新後在頁面頂端出現一行「有新內容，重新整理套用」的提示條，不做強制 reload。"
 },
 {
  "name": "渲染效能：大狀態物件不能被高頻更新推著跑",
  "rationale": "模板已經踩過這個坑並留下註解——倒數計時如果每秒 setState，會讓 800 行的 renderVals 全量重算，所以改成直接寫 DOM。內容站的 renderVals 只會更大，這條紀律必須從第一天就守。",
  "concrete_ui": "高頻更新（倒數秒數、卡片翻面動畫、作答計時、捲動進度條）一律直接操作 DOM 或用 CSS 動畫，不進 state。章節正文以 dangerouslySetInnerHTML 一次掛入（模板既有做法），內部互動（例題展開、答案遮罩）用事件委派而非為每個元素建 React 節點。長列表（考古題索引、錯題本）用虛擬捲動。圖片標 width/height 避免 CLS，數學式用自帶 KaTeX（放 vendor/，比照模板自帶 React 不依賴 CDN 的做法）或直接預渲染成 SVG。"
 },
 {
  "name": "SEO 與可分享性靠靜態 shell 與明確的 canonical",
  "rationale": "全站 catch-all 到單一 app.html 的 SPA 對搜尋引擎不友善，但這是這個技術模板的既定形狀。折衷是在建置期為每個節產生一份含正文與 meta 的靜態 HTML，SPA 在客端接手；同時嚴格控制 canonical，避免同一份內容被多個網址稀釋。",
  "concrete_ui": "建置腳本從 data/{subject}/{chapter}.js 產生 /{subject}/{chapter}/{section}/index.html，內含 title、description、og:image（用該科色系自動產圖，比照模板 B30 圖卡的 canvas 產圖做法）、JSON-LD（Article ＋ BreadcrumbList；考古題頁用 Quiz/Question）。canonical 規則：?view=cards 與 ?view=quiz 一律 canonical 回無 query 的節頁；/q/{qid} 短連結 301 到 /past-papers/{year}/{subject}/{no}；/social 與 /science 是 hub 頁不是內容頁，其 canonical 指向自己但不與子科競爭關鍵字。分享卡片要帶科目色與麵包屑文字，讓學生貼到群組時一眼看得出是哪一科哪一節。"
 },
 {
  "name": "時效性資料要標年度、標來源、標最後更新",
  "rationale": "級分對照、篩選倍率、招生名額、申請時程每年都變，而這正是搜尋流量最大也最容易誤導人的一塊。一份沒標年度的落點試算比沒有還糟。這也對應模板 README 那條紀律——data/history/ 只能增加不能刪改，時序資料過了就補不回來。",
  "concrete_ui": "每個 data/exam/*.js 與 data/guide/*.js 的檔頭都有 meta：year、source_url、fetched_at、license。所有吃這些資料的頁面在顯著位置（不是頁尾小字）顯示「依據 {year} 學年度資料 · 最後更新 {date} · 來源 {大考中心}」。落點試算的結果一律以區間與「僅供參考」呈現，不給精確錄取機率。歷年資料只增不改，舊年度保留為可切換的檢視。/about/sources 由這些 meta 自動生成，不手寫。"
 }
]

## risks
[
 "內容生產才是真正的瓶頸，不是工程。六科上千節的正文、複習卡與考古題詳解，工作量遠大於整個前端；架構若假設內容會自己出現，站會長成一個空殼。建議先用單科（數A）走完整條閉環驗證形態，再橫向複製。",
 "考古題的著作權界線。試題本身多可引用，但詳解必須自製，且不能整批搬運他站或補習班的解析；圖表題涉及第三方圖片授權。上線前需要逐年度逐科確認可用範圍。",
 "級分換算與落點試算的誤導責任。學生會拿它做真實志願決策，一個過期的對照表或一句被讀成保證的措辭都可能造成實質傷害。必須有年度標示、區間呈現、免責說明，且每年招生資料釋出後有明確的更新負責人。",
 "AI 助教的幻覺在教育場景代價特別高——編出一個不存在的公式或錯誤的制度規定，學生會直接相信。務必綁章節脈絡、強制附出處連結，並對「制度、時程、名額」這類事實性問題改走檢索而非生成。",
 "單檔 HTML 的膨脹上限。模板 app.html 已達 1.27MB，內容站若不嚴守「殼歸殼、內容歸分片」的分界，半年內就會回到首屏數秒的狀態。需要在 CI 加上 app.html 體積門檻，超過就擋 PR。",
 "SPA catch-all 路由對 SEO 的先天劣勢。若靜態 shell 的預先產生沒做，或做了但與 SPA 客端渲染內容不一致（軟性偽裝的風險），搜尋排名會直接崩掉，而搜尋正是這個站最大的流量來源。",
 "學生成績屬於高度敏感個資，且使用者多為未成年。模考成績、弱點分析、落點資料一旦外洩或被用於非預期用途，後果嚴重。需要最小化蒐集、明確的刪除與匯出機制，並審視是否觸及未成年個資的法遵要求。",
 "社群功能的維運負擔被低估。未成年使用者、洗版、代寫答案、考試作弊協作，都需要真人審核與檢舉流程。建議延後，且只做綁在章節底下的提問形態，不開自由發文看板。",
 "Service Worker 造成的內容不同步。學生讀到的是三週前快取的舊版詳解卻不自知，是這類站最難查的客訴。需要明確的版本提示條與強制淘汰機制，且版本戳記（tools/stamp-assets.py）不能漏跑——README 已把這條列為部署前必做。",
 "D1 的寫入量與成本。複習卡排程與作答記錄是高頻寫入，一萬名活躍學生每天數十萬筆，遠超模板現有的訂閱與通知量級。需要客端批次合併後上傳、而非每次翻卡都打一次 API。",
 "六色系統的無障礙風險。六個色相在色覺缺陷者眼中可能塌成三組（特別是 mmj 綠與 vbs 紅橘），若顏色成為唯一的科目線索就會失效。圖示與文字標籤必須是主要區辨，顏色只是輔助。",
 "三層路由的 slug 一旦公開就等於契約。/math-a/polynomials/remainder-theorem 被學生存成書籤、貼進群組、被搜尋引擎收錄之後就不能隨意重整章節結構；需要一開始就定好 slug 命名規範，並保留一張永久的舊網址轉址表（比照模板對分享碼格式不可變更的紀律）。"
]

## title
學測複習整合站：資訊架構與功能地圖（沿用 Project SEKAI 資源中心技術模板）
