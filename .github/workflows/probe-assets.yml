name: probe-assets (temporary)
on:
  push:
    branches: [claude/b30-moyu-table-page-dn9zkp]
    paths: ['tools/probe-assets.sh', '.github/workflows/probe-assets.yml']
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash tools/probe-assets.sh
